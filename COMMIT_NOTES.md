Commit 1: feat(simulation): telemetry-driven weighted efficiency and AI progress smoothing
- Implemented weighted efficiency formula using power, thermal loss, pressure, and valve scores.
- Added per-factory AI progress smoothing to simulate gradual telemetry improvements when AI is enabled.
- Exposed `plantEfficiencyAvg` and per-factory `aiProgress` for UI.

Commit 2: docs: add commit notes file (this file)

Commit 3: chore: add CHANGELOG entry for telemetry improvements
